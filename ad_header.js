/* Decoy asset for the session-check measurement (see the inline snippet before
   </body> on each page). Intentionally a no-op: if this file loads, the
   browser is not filtering ad-patterned URLs; if it is blocked, the loader's
   onerror fires. Do not rename — the filename matching generic filter-list
   rules is the entire point. */
window.__ilAdHeaderLoaded = true;
